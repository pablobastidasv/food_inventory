package inventorymanager_test

import (
	"testing"
)

func Test_ProductCreator(t *testing.T) {
	// t.Run("given a product, when it's created, then no error is returned", func(t *testing.T) {
	// 	input := inventorymanager.CreateProductInput{
	// 		Id:       uuid.NewString(),
	// 		Name:     gofaker.Name(),
	// 		Category: "VEGETABLES",
	// 	}
	//
	// 	_, err := sut.CreateProduct(context.Background(), input)
	//
	// 	assert.NoError(t, err)
	// })
}
